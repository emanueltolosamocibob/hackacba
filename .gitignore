node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
supabase/.branches
supabase/.temp
.vercel

# El motor de impeccable: 15 MB por copia y solo windows-x64. El launcher lo
# busca en ~/.impeccable/bin o lo baja a su cache, asi que no va en git.
.claude/skills/impeccable/scripts/bin/
.cursor/skills/impeccable/scripts/bin/

# Capturas de la ronda de inspeccion de impeccable: 2 MB regenerables con
# `node capturar.mjs`. El surface brief de al lado si se commitea.
paquetes/web/.impeccable/review/
